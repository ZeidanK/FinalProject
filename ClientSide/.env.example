# Frontend API base URL.
# Local development should usually stay on /api so Vite proxy forwards to http://localhost:5050.
VITE_API_BASE_URL=/api

# Production in this project should target the backend path below.
# Example production value: /cgroup4/test2/tar1/api
